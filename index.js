function myFunction() {

      for (var x=0; x<=100; x++) {

          if (x === 0) {
                  document.write("Dallas ");

          }
          else if (x % 2 === 0 ) {
                  document.write(" Boys")
          }

          else  {
                  document.write(" Cow")
          }

          if (x % 3 <= 0 ) {
             document.write(" Cowboys")
          }
        }
}
